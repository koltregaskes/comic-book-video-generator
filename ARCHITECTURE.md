# Architecture

- React + Vite + TypeScript
- localStorage draft persistence
- `creative-project-package-v1` output with `projectType: "comic-book-video"`
- import adapter that accepts either a motion-comic package or a `comic-book` package

Core files:

- `src/project-package.ts`
- `src/sample-project.ts`
- `src/App.tsx`
